using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class AIVisionDetectionBehavior : AIBehaviorBase
{
    public override void UpdateBehavior(NavMeshAgent agent)
    {
        if (isActive)
        {
          
        }
    }
}
